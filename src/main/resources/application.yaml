spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3309/lab?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: lab
    password: labpass
  jpa:
    hibernate:
      ddl-auto: update
  logging:
    level:
      org.springframework.transaction: trace
      org.hibernate.orm.jdbc.blind: trace
      org.hibernate.SQL: DEBUG
      org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: TRACE